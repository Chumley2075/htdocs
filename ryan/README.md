# Ryan Project Guide

Start here if you are new to this repo section.

## Key docs
- Beginner crash course: `docs/CRASH_COURSE_ONE_PAGER.md`

## Main folders
- `app/` = classroom app source (`core/`, `controllers/`, `pages/`)
- `yilma/` = portal + face-recognition services

## Important note
- Root PHP files in `ryan/` and `ryan/yilma/` are mostly route/URL shims.
- For behavior changes, edit canonical files in `app/*` first.
